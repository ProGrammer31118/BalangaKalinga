{
  "name": "balanga-kalinga",
  "version": "1.0.0",
  "private": true,
  "description": "Balanga Kalinga - AI-assisted mental assessment & support platform for students",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix server/client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix server/client\"",
    "build": "npm run build --prefix server/client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
