{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install --prefix server && npm install --prefix server/client",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index.js" }],
  "functions": {
    "api/index.js": { "maxDuration": 30 }
  }
}